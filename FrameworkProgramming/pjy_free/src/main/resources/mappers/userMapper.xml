<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.tukorea.myweb.mapper.UserMapper">
  

     <insert id="insert"> 
   		insert into user (id, passwd, username, birth, mobile, email) values
 		(#{id}, #{passwd}, #{username}, #{birth}, #{mobile}, #{email})
     </insert> 
 	
     <select id="selectByid" resultType="UserVO">
 		select * from user where id = #{id}
     </select>
 	
     <update id="update">
	 	update user set id =#{id}, passwd =#{passwd}, username =#{username}, birth =#{birth}, mobile =#{mobile}, email =#{email} 
		 where id = #{id}
    </update>
 
    <delete id="delete">
		delete from user where id = #{id}
    </delete>
     

	
</mapper>  